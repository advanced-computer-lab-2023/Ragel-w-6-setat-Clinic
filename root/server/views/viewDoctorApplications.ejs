<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <style>
    body {
      background-color: #b3e5fc;
      color: #212121;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .doctor-list {
      background-color: #0288d1;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      width: 80%;
      text-align: center;
      overflow: auto;
    }

    .doctor-item {
      background-color: #ffffff;
      color: #212121;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
    }
  </style>
</head>

<body>
  <%- include("adminNavBar", {adminId: userId}) %>
    <div class="doctor-list">
      <h1>Unregistered Doctor Applications</h1>
      <% doctors.forEach(doctor=> { %>
        <div class="doctor-item">
          <p><strong>Username:</strong>
            <%= doctor.username %>
          </p>
          <p><strong>Email:</strong>
            <%= doctor.email %>
          </p>
          <p><strong>Full Name:</strong>
            <%= doctor.fName %>
              <%= doctor.lName %>
          </p>
          <p><strong>Date of Birth:</strong>
            <%= doctor.dateOfBirth.toDateString() %>
          </p>
          <p><strong>Education:</strong>
            <%= doctor.educationalBackground %>
          </p>
          <p><strong>Hourly Rate:</strong>
            <%= doctor.hourlyRate %>
          </p>
          <p><strong>Session Price:</strong>
            <%= doctor.sessionPrice %>
          </p>
          <p><strong>Affiliation:</strong>
            <%= doctor.affiliation %>
          </p>
          <p><strong>Specialty:</strong>
            <%= doctor.specialty %>
          </p>
        </div>
        <% }); %>
    </div>
</body>

</html>