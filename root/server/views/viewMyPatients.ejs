<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Patients</title>
    <style>
        body {
            background-color: #0288D1;
            color: #ffffff;
            font-family: Arial, sans-serif;
            padding: 20px;
            margin: 0;
        }

        h1 {
            text-align: center;
        }

        .search-container {
            margin-bottom: 20px;
        }

        .search-input {
            padding: 10px;
            border: none;
            border-radius: 5px;
        }

        .search-button {
            padding: 10px 20px;
            background-color: #03a9f4;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .patient-item {
            background-color: #B3E5FC;
            color: #212121;
            padding: 20px;
            margin: 10px 0;
            border-radius: 10px;
        }

        .divider {
            border-top: 1px solid #bdbdbd;
            margin: 20px 0;
        }
    </style>
</head>

<body>
    <%- include("doctorNavBar", {doctorId: userId}) %>
        <h1>List of Patients</h1>

        <form action="/doctors/searchForPatients/<%= userId %>" method="get">
            <div class="search-container">
                <input class="search-input" name="fName" type="text" id="searchInput"
                    placeholder="Search by first name...">
                <input class="search-input" name="lName" type="text" id="searchInput"
                    placeholder="Search by last name...">
                <button type="submit" class="search-button">Search</button>
            </div>
        </form>

        <form action="/doctors/upcomingappointments/<%= userId %>" method="get">
            <button type="submit" class="search-button">Show Patients With Upcoming Appointments</button>
        </form>

        <% if (patients && patients.length> 0) { %>
            <% patients.forEach(patient=> { %>
                <div class="patient-item">
                    <a href="/doctors/selectedPatient/<%= userId %>/<%= patient._id %>">
                        <h2>
                            <%= patient.fName %>
                                <%= patient.lName %>
                        </h2>
                        <p><strong>Email:</strong>
                            <%= patient.email %>
                        </p>
                        <p><strong>Date of Birth:</strong>
                            <%= patient.dateOfBirth.toDateString() %>
                        </p>
                        <p><strong>Gender:</strong>
                            <%= patient.gender %>
                        </p>
                        <p><strong>Phone Number:</strong>
                            <%= patient.phoneNum %>
                        </p>
                        <p><strong>Emergency Contact:</strong>
                            <%= patient.emergencyContact.fName %>
                                <%= patient.emergencyContact.lName %> (<%= patient.emergencyContact.phoneNum %>)
                        </p>
                    </a>
                    <div class="divider"></div>
                </div>
                <% }); %>
                    <% } else { %>
                        <p>No patients found in the database.</p>
                        <% } %>


</body>

</html>